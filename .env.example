# Copy to .env and fill in your values
# NEVER commit .env to git

# --- API provider (pick one) ---

# Option A: OpenRouter (recommended)
OPENROUTER_API_KEY=sk-or-...

# Option B: Direct Anthropic
# ANTHROPIC_API_KEY=sk-ant-...

# Telegram bot token (from @BotFather)
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...

# OpenClaw gateway token (auto-generated if not set)
# OPENCLAW_GATEWAY_TOKEN=

# Telegram user IDs allowed to message the bot (comma-separated)
# Find your ID: message @userinfobot on Telegram
TELEGRAM_ALLOWED_IDS=

# Telegram group IDs (optional, comma-separated) — enables the bot in group chats
# Groups must have topics enabled and the bot must be an admin with privacy mode disabled
# TELEGRAM_GROUP_IDS=-1003782901451

# OpenAI API key (optional — required for TTS and Codex ACP sessions)
# OPENAI_API_KEY=

# GitHub personal access token (optional — enables gh CLI on remote VM)
# Create at https://github.com/settings/tokens (classic, repo scope)
# GH_TOKEN=

# Brave Search API key (optional — enables web search tool)
# Get one at https://brave.com/search/api/ (Data for Search plan)
# BRAVE_API_KEY=

# Slack tokens (optional — enables Slack channel)
# Create a Slack app at https://api.slack.com/apps, enable Socket Mode,
# and install to your workspace.
# SLACK_APP_TOKEN=xapp-...   (App-Level Token with connections:write scope)
# SLACK_BOT_TOKEN=xoxb-...   (Bot User OAuth Token)

# Tailscale auth key (optional — enables Tailscale SSH access to the VM)
# Generate at https://login.tailscale.com/admin/settings/keys
# TAILSCALE_AUTHKEY=

# State repo for workspace backup/restore (optional)
# Private repo (SSH URL) used to restore workspace on fresh volumes.
# Agent pushes backups here periodically; entrypoint restores on volume loss.
# STATE_REPO=git@github.com:user/agent-state.git

# Config repos for ACP harness CLIs (optional — defaults shown below)
# Skills, hooks, agents, and settings are seeded from these on first boot.
# CLAUDE_CONFIG_REPO=https://github.com/dtbuchholz/claude-config.git
# CODEX_CONFIG_REPO=https://github.com/dtbuchholz/codex-config.git
